# myLinear P2产品模块设计

> **阶段范围**（对应 README 路线图 P2）：视图体系三件套 + Views 页——任务/项目列表页的 Filter 面板、Display options 面板、saved_view 视图持久化（页面级 view tab 与 workspace 级 Views 页双轨隔离），对齐 Linear。
> **上游依据**：[DATABASE_DESIGN.md](../../DATABASE_DESIGN.md)（v1.3 → **v1.4 迁移：saved_view 加 surface 列 + project_id 列**）、[DESIGN-linear.app.md](../../DESIGN-linear.app.md)、[P0.md](./P0.md)、[P1.md](./P1.md)。
> **本阶段不含**：owner/visibility 私有共享视图（无登录体系，留 P4）；跨重启状态保持（Linear 靠服务端 per-user 状态；本项目扩展点为 localStorage，登记不做）；多字段组合排序；分组折叠；看板（P3/P4）；展示优化批与图标选择器等（P3，见 §1.10）。
> **文档性质**：阶段快照，定稿后不再修改。接口级契约见 [../api.md](../api.md)（§10 View 接口、§7/§8 列表过滤参数改版）。

---

## 1. 总决策

1. **实施顺序**（frontend-driven）：Filter 面板（后端过滤契约 + 面板 UI）→ Display options 面板（纯前端）→ 视图持久化（saved_view CRUD + 页面 tab）→ Views 页 → 验证回写。面板是本体，视图只是面板状态的持久化壳。
2. **状态存放（方案 A，2026-09 拍板）**：filter 状态进 URL（延续现有 `?filter=` tab 的同步能力并扩展为面板条件编码）；display options 纯前端内存、刷新重置、可被 view 固化；项目列表表头排序**前端化**，退掉 `GET /projects` 的 `sort`/`order` 后端参数，ordering 单一路径（表头点击与面板 Ordering 共享同一前端状态）。任务列表 SQL 固定 ORDER BY 保留为"无 ordering 时的基底序"，前端排序覆盖其上。
3. **行完备原则延续**（api.md §2.9）：display 控制（分组/排序/展示属性/子任务开关）是前端对已取回平铺行集的内存操作，切换不发请求；**仅 filters 翻译成列表接口查询参数发请求**。响应形状零变更。
4. **tab 与面板状态统一（A2 实施修订，取代原"tab = status 条件 UI 糖"方案）**：Active/Backlog/All tab 是前端**隐式基底作用域**——独立 URL 参数 `?tab=`（不显示为 chip、不进 f=），请求时合成 `f= = tab 基底条件 + chip 条件`（AND 叠加；Active = `status.isAnyOf.todo,in_progress`、Backlog = `status.is.backlog`、All = 不附加）。切 tab 只改写 tab 键保留 f=；面板 status 条件与 tab 互不覆写。后端 `filter=` 遗留别名随之退役（删除 `legacyStatusConds` 与 handler 分支）。原案"点 tab = 覆写 status 条件、tab 按条件是否恰等于预设决定高亮"废弃：覆写会破坏用户手加的 status chip，高亮判定复杂且 Linear 实测 tab 与 chip 层独立。
5. **saved_view 双轨隔离**：加 `surface` 列（`tasks_page` / `projects_page` / `views_page` / `project_issues`）。页面级 view 只出现在对应列表页 tab 行；Views 页只列 `views_page` 的 view（对齐 Linear 实测行为：两处互不可见）；`project_issues` 面 view 带 `project_id` 做项目级隔离（§4.1）。
6. **操作符语义（定稿 v2，2026-09 Linear 实测真值表）**：操作符集合随选中值个数联动（UI 层控制菜单展示与文案，wire 承载全量操作符）：

   | 字段类型 | 选中值个数 | 操作符（wire） | 集合语义 |
   |----------|----------|--------|----------|
   | 单值字段（status / priority / assignee / lead / project） | 1 值 | `is` / `isNot` | 值 = / ≠ 所选值 |
   | | ≥2 值 | `isAnyOf` / `isNot` | 值 ∈ 集合（OR）/ ∉ 集合（NULL/空伪值行按伪值参与判定，见 §2.3） |
   | 多值 labels 族（labels） | 1 值 | `inclAll` / `exclAny` | include / do not include |
   | | ≥2 值 | `inclAll` / `inclAny` / `exclAny` / `exclAll` | 所选 ⊆ 行集合 / ∩ ≠ ∅ / ∩ = ∅ / ⊄ 行集合 |
   | 多值 members 族（member） | 任意 | `inclAny` / `exclAny` | contains any / does not contain（文案随值个数切 contains / contains any） |
   | 日期字段 | 单值 | `before` / `after`（`is`/`isNot` 承载 none 空值条件） | 相对阶梯 cutoff 比较（§2.4） |

7. **组合语义**：条件为**有序列表**（同字段可重复出现多条 chip，图13 两个 Status），整表 AND；面板条件与 URL 中其他条件同层 AND。
8. **三层状态模型（filter/display/view 通用，2026-09 Linear 实测定稿）**：currentState（页面活状态 = filters 条件列表 + display，filters 镜像进 URL）是唯一真相源；view 是服务端某时刻 currentState 的快照（config 深拷贝）；编辑态再叠一层 draft。**任何修改只动顶层状态，永不自动写回 view.config**——写回只发生在三个显式动作：保存新视图 / Save to this view / 编辑态 Save。
9. **view 激活下的两条修改路径（2026-09 Linear 实测 + 用户拍板定稿；编辑 panel 形态与联动语义定稿）**：不进编辑态 = 直接改 currentState、列表实时响应、tab 显示偏离点、chip 行 Save 升级为分裂下拉（Save to this view = PATCH config ← currentState；Create new view... = 开新建 panel 保存后切换激活）；**编辑态 = 行内编辑 panel**（入口两个：tab 行 "+" 新建 / view tab 右键 Edit 编辑已有；**SaveViewDialog 弹窗方案退役**）。panel 形态（Linear 实测）：name 行（图标 + name 输入 + 右靠 Cancel/Save）+ Description 行 + chip 行（行尾右靠 filter/display 双按钮）。**编辑期是隔离预览沙箱**：panel 内 filter 按钮与顶部 filter 按钮**操作同一 draft 条件列表**（chip 只落 panel chip 行一处，顶部 chip 行编辑期隐藏；用户 2026-09 拍板：Linear 原设计编辑期顶部按钮写 invisible transient 集、条件不可见且易 AND 出空列表，弃）；panel 内 display 按钮与顶部 display 按钮同操作同一 draft display（实测等效）。**预览口径 = 仅 draft 条件**：进入编辑前的 transient 状态**挂起**（不叠进预览、编辑期无 hidden-by-filters 提示），Save/Cancel 后恢复叠加。**draft 初值**：新建 = filters 空 + display 继承当前页面状态（所见即所存）；Edit = view.config 深拷贝。**Reset 三档**：预设 tab = 全局默认（现状）；view tab = 该 view 保存的 config.display；编辑期 = 进入编辑时基线（新建 = 全局默认 / Edit = config.display）。Save = 元数据+config 一起提交、view 激活、transient 恢复；Cancel = draft 全弃、transient 恢复。带偏离切 tab/导航 = 丢弃偏离（filters 部分随 URL 走）。
10. **展示优化批后置 P3（2026-09 用户拍板；Sub-grouping / Show empty groups / Show closed projects 三项已于 B 切片提前落地 projects_page 面 → 详见 §3 as-built 注）**：Sub-grouping 二级分组、Show empty groups、组序方向切换、hidden-by-filters 隐藏计数空态、视图图标选择器（P2 tab 用固定层叠图标）；同批裁剪项：逐值命中计数、AI filter / Advanced filter、菜单搜索框、Board/Timeline 布局段、Show closed projects、Duplicate/Favorite/Copy link、Custom date 档。

## 2. Filter 面板

### 2.1 入口与形态（Linear 实测定稿）

- 页头右侧 filter 按钮（圆形漏斗图标，页头三按钮统一圆形形态）→ **纯添加**级联菜单（shadcn DropdownMenu 实现，二级面板经 Radix Sub **悬浮飞出**）：一级字段清单 → 二级值多选勾选清单（单值/多值字段）或日期字段清单（Dates）→ 三级相对阶梯；菜单**不显示已有条件**（同字段再加一条 = 新条件 chip，§1.7）。
- 添加后的条件落在 **chip 行**（tab 行与表头之间）：chip 三段可点——字段（静态）/ 操作符（点开该字段随值个数联动的操作符清单切换）/ 值（重开多选；日期字段为阶梯单选清单）——加 × 删除；行尾 `+` 添加、`Clear` 全清、`Save` 进视图保存流程（§4）。
- 添加时默认操作符：单值 = is、labels 族 = inclAll、members 族 = inclAny、日期随阶梯方向（ago = after / from now = before，§2.4）。操作符文案随选中值个数切换（is → is any of、include → include all of 等）；值个数变化时操作符等价归一（isAnyOf 单值 → is 等）。
- 多值字段 none 哨兵与真实值**互斥**（勾 No labels 即取消其它勾选）；单值字段可混选（No lead + 真人 = 未指派 OR）。
- 值行**分两区**（Linear 界面实测定稿）：checkbox 勾选区点击 = 本会话累积勾选（多选自动**合并为一个 chip**，菜单**不关闭**，可继续勾该属性其它选项或切其它属性）；其余区（文字）点击 = 单值直接成**一条 chip** 并**关闭整个菜单**（不再有机会点第二个选项）。
- 多选子面板贴近视口右缘时 Radix 会把飞出清单 popper 翻到 side=left，触发 onItemLeave 抢焦点误关 Sub——用**悬挂守卫**（指针仍在飞出清单内时忽略 focus-outside）阻断误关，指针移开其它字段行仍照常互斥关闭。
- filter / display / panel 收缩三按钮统一圆形形态；filter / display 按钮在非默认状态时亮蓝点。

### 2.2 可过滤字段矩阵

**Tasks 页**：

| 字段 | 类型 | 值来源 | 空伪值 |
|------|------|--------|--------|
| status | 单值 | 5 枚举 | — |
| assignee | 单值可空 | 成员 | No assignee |
| priority | 单值 | 0-4 | No priority（= 值 0，非 NULL） |
| labels | 多值 | scope=task 标签 | No labels |
| project | 单值可空 | 项目 | No project（仅 tasks_page 面；project_issues 面隐含不提供） |
| dueDate | 日期（from now 阶梯仅五档 `1d/3d/1w/1mo/3mo`） | 相对阶梯 | No due date；另提供 **Overdue** 逾期谓词（§2.4） |
| createdAt / updatedAt | 日期（ago 阶梯七档） | 相对阶梯 | — |

> Started date / Complete date / Auto-closed / Triaged / Time in current status 不做（无 activities 记录，task 表无对应列）。

**Projects 页**：

| 字段 | 类型 | 值来源 | 空伪值 |
|------|------|--------|--------|
| status | 单值 | 5 枚举 | — |
| priority | 单值 | 0-4 | No priority |
| lead | 单值可空 | 成员 | No lead |
| member | 多值（members 族操作符） | 成员 | — |
| labels | 多值（labels 族操作符） | scope=project 标签 | No labels |
| createdAt / updatedAt | 日期（ago 阶梯七档） | 相对阶梯 | — |
| startDate | 日期（from now 阶梯七档） | 相对阶梯 | No start date |
| targetDate | 日期（from now 阶梯仅三档） | 相对阶梯 | — |

> 默认补入项定案（2026-09 Linear 实测真值表）：task 的 project 过滤、No assignee 均实施；No members 依 Linear 实测撤销（members 字段不提供空伪值选项）。

**项目详情 Issues tab**（第三个列表面，`GET /projects/:projectId/tasks`）：共享 Tasks 页字段矩阵与后端白名单（同 `f=` 契约）；project 字段在本面隐含（行必属当前项目），前端 Filter 菜单不提供。**前端 Filter 已接通（2026-09 A2 切片完成）**：页头 actions 区 filter 按钮（置于折叠面板按钮左侧、仅 Issues tab 显示）+ 列表区上方 chip 行（bare 样式，随右侧面板收窄对齐）。本面视图归属已定案（§4.1：surface=`project_issues` + project_id 项目级隔离，2026-09）。

### 2.3 空伪值契约

可空单值字段（assignee / lead / project）与多值 labels 族字段（labels）的值选择器提供空伪值选项（No assignee 等），wire 上编码为哨兵值 `none`；日期字段 startDate 亦提供 No start date：

- 单值可空：`none` ↔ NULL 双向映射；`isNot [x...]` 时 NULL 行按"伪值 ∉ 集合"判定 → **包含 NULL 行**（"assignee 不是 Lee"包含未指派任务，与 Linear 直觉一致）。单值字段 none 与真实值可混选。
- 多值：`none` 选中 = 空集合行；`inclAll [none]` = 无标签/无成员的行；`exclAny [none]` = 至少有一个标签/成员的行。**none 与真实值互斥**（勾 No labels 即取消其它勾选，Linear 实测）。
- members 字段不提供 none 选项（Linear 实测真值表）。
- priority 的 No priority 是真实值 0，不走哨兵。
- 日期空值条件走 `is`/`isNot` + `none`（No start date / No due date）；before/after 不参与空值判定。

### 2.4 日期相对条件

条件 = {字段, before|after, 阶梯码}（空值条件 = {字段, is|isNot, none}；逾期谓词 = {dueDate, is|isNot, overdue}，阶梯清单置顶行，chip 读作 "Due date is Overdue"；is = 非空且早于今天，今天到期不算逾期）。阶梯**按字段分方向**（Linear 实测真值表）：createdAt / updatedAt = ago 七档 `1d / 3d / 1w / 1mo / 3mo / 6mo / 1y`；startDate = from now 七档（同码未来偏移）+ No start date；targetDate = from now 仅三档 `3mo / 6mo / 1y`；dueDate = from now 仅五档 `1d / 3d / 1w / 1mo / 3mo`（Linear 实测无 6mo/1y）+ No due date + Overdue（Custom date or timeframe 后置 P3）。cutoff 由 **Go 层计算**（可测试）：timestamptz 列（createdAt/updatedAt）cutoff = now()±offset 比时刻；date 列（dueDate/startDate/targetDate）cutoff = today±offset 比日历日（civil.Date 算术；ago 取 −、from now 取 +）。`before` = 早于 cutoff、`after` = 晚于 cutoff。添加默认操作符：ago 档 = after（"晚于 N 前" = 近 N 内）、from now 档 = before（"早于 N 后" = 未来 N 内），chip 可切。

### 2.5 URL 编码与后端求值契约

- URL：重复查询参数 `f=<field>.<op>.<v1,v2>`，如 `f=status.isAnyOf.todo,in_progress&f=labels.inclAll.l1,l2&f=dueDate.after.3d&f=assignee.is.none&f=startDate.is.none`；解码白名单校验，非法条目静默丢弃（回退无该条件）。
- **求值层：Go 层条件列表引擎（internal/filter 包），2026-09 修订原"下沉 sqlc 数组参数"方案**。修订理由：条件列表模型条数动态（同字段可重复），sqlc 固定形状查询无法承载；多值字段重复 include 条件的 AND 无法折叠为单个数组参数（incl A ∧ incl B ≠ incl A∪B）；不分页前提下全量行集本就取回内存，labels/members 联结数据可在求值前批量取齐，与既有"动态排序放 Go 层"约定（api.md §2.2）同构，NULL/伪值语义单点可测试。
- 引擎契约：`filter.Parse`（f= 数组 + 实体字段白名单 Spec，非法条目静默丢弃）→ `filter.Apply`（行集 + 条件列表 + 实体字段值提取器，条件间 AND）。SQL 只保留基础谓词（workspace + 软删）与基底序；`ListTasksByWorkspace` 的 statuses 数组参数退役（handler 恒传非 nil 空切片，随后续查询清理删除）。
- 求值联结数据：labels 走既有 `ListLabelsByTaskIds` / `ListLabelsByProjectIds`；项目 member 集合走新增 `ListProjectMemberIdsByProjectIds`（仅 member 条件存在时批量取）。
- 遗留参数：`filter=`（tasks）**已退役**（2026-09 A2 前端 tab 迁移完成，后端删除 `legacyStatusConds` 与 handler 分支；tab 作用域由页面侧合成进 f=，§1.4）；~~`sort=`/`order=`（projects）保留至 display options 切片前端化 ordering 时退役~~ **已退役**（2026-09 B 切片 ordering 前端化完成，后端删 `sort`/`order` 参数与 `sortProjects`，`TODO(P2-B)`；api.md §2.2 / §7 已回写）。
- React Query 键含 `f` 参数 → 过滤切换发请求；display 切换不进键、不发请求。

### 2.6 树形视图节点展示规则（2026-09-14 修订④成文定稿，用户 Linear 实测定稿）

**模式判定**（display 双开关，§3.4）：Show sub-issues 关 = 仅一级行；开 + Nested 关 = 平铺；开 + Nested 开 = 树形。本节主规则仅适用于**树形模式**；平铺模式见下「其它口径」。

**概念定义**：

- **基础行集**：后端取回的全部行（含已完成任务——链条/孤儿判定需要它们在场）。
- **展示行集**：基础行集经 Completed tasks 档与 Show sub-issues 档过滤后的行（flat 行 / 组头计数 / 树锚点同源于此）。
- **锚点**：展示行集中自身值命中当前组路径（一级组值 + 二级组值）的行；Completed=none 时已完成任务**不作锚点**。
- **渲染集（kept）**：组内实际渲染的树节点集合 = **锚点祖先链 ∪ 锚点完整子树**。

**主规则（规则④）**：

- **规则A（祖先链）**：锚点的全部祖先进入渲染集——非锚点祖先为**链条节点**，渲染并置灰。
- **规则B（完整子树）**：锚点子树内任意深度的后代，即使非锚点也进入渲染集并置灰（Linear 实测 sub2-2-1 / task8-1 例）——**子树展开权仅属锚点**。
- **侧分支剪枝**：链条节点的子树中不含锚点的分支（∉ 任何锚点子树）整体剪枝不渲染（Linear 实测 sub2 / task3 / gRPC 例）。

**Completed tasks = none 叠加层**（本产品档位，叠加在规则④上）：

- 已完成任务不作锚点，仅经规则A 以链条节点身份进入渲染集（置灰；task1 例）。
- 锚点子树内的已完成叶子：自身不入渲染集。
- 锚点子树内的已完成中间节点：自身不入渲染集，但**不阻断向下展开**（其下后代仍可经规则A/B 进入；task4 例）。链条上溯遇此情形须补全中间节点（实现层以独立 chained 集合判终止，不得以 kept 成员身份判，否则内层锚点先经规则B 入集时会跳过链条补全导致断链）。

**节点命运速查表**（组内任一节点 N）：

| # | N 的形态 | 命运 |
|---|----------|------|
| 1 | N 是锚点 | 渲染，正常亮度 |
| 2 | N 非锚点，但子树中含锚点（链条节点） | 渲染，置灰 |
| 3 | N 非锚点，是某锚点的任意深度后代（规则B） | 渲染，置灰；若 Completed=none 且 N 已完成 → 自身不渲染（不阻断其下后代入集） |
| 4 | N 非锚点，子树无锚点，且不属于任何锚点子树（侧分支） | 整体剪枝 |
| 5 | N 是孤儿（父不在基础行集） | 提为树根渲染，置灰 |

**其它口径**：

- 不分组（No grouping）树形同走渲染集：锚点 = 展示行集本身（Completed=all 不剪枝；=none 剪纯已完成子树）。
- **平铺模式不插父行**（用户定案）：仅渲染展示行集中自身命中的行，有父的行标题后带 “› 父标题” 面包屑（父被过滤/隐藏时孤儿同样带面包屑，延续 P0 行为）。
- Show sub-issues 关：仅一级行；父不在结果集的孤儿视为一级行渲染（避免行凭空消失）。
- 组头计数 = 锚点行数（flat/tree 同口径），不随剪枝/链条变化。
- 子树进度徽标 x/y = 真实子树口径（done 数 / 总数），不受剪枝影响。
- 置灰实现为 opacity-60；三条件并联：孤儿 / Completed=none 下的已完成链条节点 / 组路径值未命中。

**修订史**：C 切片初版（命中即整棵子树展示）→ ② 锚点最小生成林 → ③ 生成林 ∪ 锚点直接子节点 → **④ 锚点祖先链 ∪ 完整子树（现行）**；③ ⊂ ④，分叉点在「锚点深度 ≥2 的非锚点后代」（③ 剪、④ 留置灰）。实现单源：[task-display-state.ts](../../myLinearFrontEnd/src/lib/task-display-state.ts) 的 `taskSpanKeptSet` / `buildTaskGroupsTree`。

## 3. Display options 面板

页头右侧第二按钮（Linear 同款滑杆图标）→ popover 四 section。**全部纯前端内存操作，不发请求、不进 URL**。

> **B 切片 as-built 修订注（2026-09，projects_page 面已落地；tasks_page 面留后续切片）**：项目列表 Display 面板实际形态自上而下——展示样式行（本切片**仅 List 一档**，Board/Timeline 裁剪）→ Grouping（一级）→ Sub-grouping（二级，**仅一级 ≠ No grouping 时出现**）→ Ordering（字段 + 升/降方向）→ Timeframe（**仅一级或二级按 start/target date 分组时出现**：Month / Quarter / Half-year / Year）→ Show closed projects（**None / All 二值**）→ **List options** section（Show empty groups 开关 + Display properties 10 chips）。**从 §1.10 P3/裁剪批提前落地**：Sub-grouping、Show empty groups、Show closed projects。**Show closed 仅 None/All 定案**：Linear 的 Past week / … 档依赖 Activity 关闭时刻，本项目无 activities 表、project 无 completed_at / canceled_at 列，故裁为二值（关闭时刻归档 = 后续优化方向）。**Show closed=None × filter = 基底 AND**：filter 先决定后端取回哪些行，display 再隐藏其中 closed 行——若过滤结果全为 closed，列表显示独立 emptyHidden 空态文案（非计数徽标）。§3.1–§3.3 字段集按本注修订。

> **C 切片 as-built 修订注（2026-09，tasks_page 面已落地）**：任务列表 Display 面板实际形态自上而下——展示样式行（仅 List）→ Grouping（**默认 Status**；行首组序拖拽入口（Linear Group ordering 面板）**后置后续优化**，本切片不渲染控件）→ Sub-grouping（同 B 切片规则）→ Ordering（**Manual** / title / status / priority / assignee / updated / created / due date）→ **Completed tasks（All / None 二值，面板行仅 All tab 展示**——其余 tab 基底条件已排除 completed；**Done 与 Canceled 都算 completed**）→ Show sub-issues 开关 → **List options** section（**Nested sub-issues 开关仅 Show sub-issues 开时展示** + Show empty groups + Display properties 8 chips）。**Order completed by recency 行不实现**（无完成时间字段），面板不渲染。**Display properties（tasks）**：status / assignee / priority / project / dueDate / labels / created / updated，**默认关 = 仅 Updated**；**ID chip 后置**（后端无 identifier/number 列，与组序拖拽、recency 同登后续优化）。**display 状态每 tab 一份**（纯内存，切 tab 不丢、刷新复位），各 tab 默认态同（Status 分组 + sub/nested 双开）。**组头计数口径** = 展示行集自身值命中（含组路径祖先值），flat/tree 同口径。**Reset 行仅非默认态渲染**（两侧面板统一）。**tree 剪枝/链条置灰/flat 不插父行按 §2.6 修订④（2026-09-14，已成文）**。§3.2–§3.4 按本注修订。

> **project_tasks 切片 as-built 注（2026-09-14，项目详情 Tasks tab 面落地）**：与 tasks_page 同一 TaskDisplayButton 面板、同默认态（Status 分组 + sub/nested 双开）；display 状态**本面一份**（纯内存，无多 tab 之分，切项目/刷新复位）；本面无 tab 基底作用域排除，**Completed tasks 行恒展示**；按钮位 = Filter 右侧、面板收缩按钮左侧，仅 Tasks tab 渲染（同 Filter 按钮规则）。过渡期 fixedDisplay（有过滤条件自动切平铺）**退役**——过滤下的孤儿/链条由 §2.6 规则④孤儿提根置灰与平铺面包屑兜底，tree/flat 完全交还用户双开关控制。

### 3.1 Grouping

- Tasks：none / status / priority / assignee / project / **label**；Projects：none / status / priority / lead / **member** / **label** / **startDate** / **targetDate**（**B 切片修订**：项目一级分组扩至与 §2.2 过滤字段矩阵同集）。
- **label 分组 = 重复入组语义**（对齐 Linear）：行在每个所属标签组各出现一次；组序 = 标签创建序；No labels 组恒最后。组头计数含跨组重复。（**B 切片**：项目 member 多值分组同此重复入组语义。）
- 其余字段空值行归 "No X" 组（恒最后）；组序：枚举按生命周期/priority 业务序，实体（assignee/lead/project）按 created_at。
- **Nesting × 分组规则**：Nesting 开时为组内嵌套——子仅在父同属该组时挂父下，否则在该组作顶层行（flat 模式带面包屑）。
- **B 切片定案（projects_page）**：① 分组值集 = 该属性 §2.2 filter 值集（priority 桶恒 No priority / Urgent / High / Medium / Low 五档等），组序 = 值集序；② status 无 “No status”（项目必有状态），priority / lead / member / label 提供 “No X” 组；③ **Show empty groups**（List options 开关）非日期字段开 = 全值集展开（无值也生成空组）、关 = 仅生成有行的组；④ **日期分组 × Timeframe**：按 start/target date 分组（一级或二级）时面板出现 Timeframe 行，桶粒度 Month / Quarter / Half-year / Year，**空日期桶永不生成**，Show empty groups 对日期字段**只决定是否展示 “No start/target date” 组**（用户 2026-09 定案：Linear 日期空组展开规则不明确，本项目从简）。
- **Sub-grouping（二级分组）B 切片已落地**（原后置 P3，§1.10 提前项）：一级选中非 none 后出现二级行，默认 No grouping，可选**剩余属性**（下拉排除一级已选字段）；折叠形态一级组头为灰底行（`bg-surface-2`）、二级组头为一条横线（`h-px bg-border`）；**一级回到 No grouping 时二级行隐藏且其值直接清除**。
- ~~分组折叠不做（与现状一致）。~~ **展示细节批③⑤已落地（2026-09）**：两级组头均可折叠（chevron，纯内存刷新重置）；组头 “+” 常显且 `sticky right-0` 钉在横向滚动视口右缘，打开创建弹窗并预填组路径值（NONE 组也展示，预填 = 仅继承）。

### 3.2 Ordering

单字段 + 方向。Tasks（**C 切片修订**）：**Manual** / title / status / priority / assignee / updated / created / due date——**Manual = 后端基底序**（`ListTasksByWorkspace` 的 `ORDER BY status 枚举序, created_at`）；非 Manual 字段前端稳定排序覆盖其上（同值保持基底序，dueDate 空值恒最后、不随方向翻转；assignee 未指派恒最后）；Projects（**B 切片修订**）：**Manual** / name / status / priority / updated / created / startDate / targetDate——**Manual = 默认排序**，后端 SQL 基底序原样呈现（`ListProjectsByWorkspace` 的 `ORDER BY created_at DESC`）；非 Manual 字段前端稳定排序覆盖其上（同值保持基底序，日期空值恒最后、不随方向翻转）。Projects 页表头点击 = 写此状态（前端内存），**表头箭头反映面板方向**；**ordering ↔ Display properties 联动**：选中排序字段对应的列在 Display properties 中自动勾选可见。Tasks 页无表头，ordering 仅面板入口。**后端 `sort`/`order` 参数与 `sortProjects` 已随本切片退役（`TODO(P2-B)` 完成，api.md §2.2 / §7 已回写）**。

### 3.3 Display properties

行内 chip/列开关，默认全开 = 现状渲染：

- Tasks 行（**C 切片修订**，8 chips）：status / assignee / priority / project / dueDate / labels / created / updated，**默认关 = 仅 Updated**；**ID chip 后置后续优化**（后端无 identifier/number 列）；
- Projects 行（**B 切片修订**：10 chips 映射 Linear）：status / priority / lead / **members** / labels / **startDate** / **targetDate** / **issues**(taskCount) / **created** / **updated**；**默认开 = 现状六列**（status / priority / lead / startDate / targetDate / taskCount），members / labels / created / updated 默认关（**P1 行完备预留项在此落地**）。列宽/网格用 inline style（`gridTemplateColumns` / `minWidth` rem）——Tailwind JIT 无法编译运行时动态 arbitrary class。

行布局遵循既有 Task list row layout 规范；开关只控制渲染与否，不改行网格契约之外的几何。**右侧元信息固定列宽槽位（2026-09-14，Linear 实测对齐）**：labels w-36 / project w-28 / dueDate w-24 / assignee w-6 / created w-24 / updated w-24，槽间 gap-4；无值行留空槽、列位不逐行漂移（流式 flex 下日期宽差与 chip 有无会推斜左邻列）；日期槽内左对齐、右缘自然参差（Linear 同款）。

### 3.4 Subtasks（仅 Tasks 页）

Show subtasks 开关 + Nesting 开关，**替换 P0 的 tab 驱动 tree/flat 硬编码**（[TaskListPage](../../myLinearFrontEnd/src/pages/tasks/TaskListPage.tsx) 的 `view={tab === "all" ? "tree" : "flat"}` 退役）：Show off = 只顶层；Show on + Nesting on = 树形；Show on + Nesting off = 平铺带面包屑。与 tab 解耦后，Active/Backlog 下也可开树形。**C 切片已落地（2026-09）**：`view=` prop 退役、TaskGroupList 改 display 状态驱动；display 状态每 tab 一份（内存），各 tab 默认 = Status 分组 + 双开关开；tree 模式孤儿（父不在结果集）提根并**置灰**（§2.6 修订）。

## 4. 视图持久化（saved_view）

### 4.1 数据与迁移（v1.4）

`CREATE TYPE view_surface AS ENUM ('tasks_page','projects_page','views_page','project_issues')`；`saved_view` 加 `surface view_surface NOT NULL DEFAULT 'views_page'` 与可空 `project_id uuid REFERENCES project(id) ON DELETE CASCADE`；索引 `(workspace_id, entity_type)` 改为 `(workspace_id, surface, entity_type)`。surface 与 entity_type 一致性由后端强制：`tasks_page ⇒ task`、`projects_page ⇒ project`、`project_issues ⇒ task ∧ project_id 必传且属同 workspace`；`views_page` 两者皆可（Views 页 tab 区分）；**project_id 仅 project_issues 面可非空**（其余面传值 400）。**项目级隔离**：项目详情 tab 行只列 `project_issues ∧ project_id=当前项目` 的 view（与预设 tab 平级）；删项目 CASCADE 其 view。

> **项目详情视图归属定案（2026-09 用户拍板）**：surface 第四值 `project_issues` + 可空 `project_id` 列即原登记候选方案；该面 "+" 创建的 view 带当前项目 id，打开项目详情时按 project_id 关联查出做 tab 行。

### 4.2 config JSON 契约（后端 opaque，前端读写）

```json
{
  "filters": [{ "field": "status", "op": "isAnyOf", "values": ["todo", "in_progress"] }],
  "display": {
    "groupBy": "priority",
    "ordering": { "field": "createdAt", "dir": "desc" },
    "visible": { "priority": true, "status": true, "assignee": true, "dueDate": true, "labels": true, "project": true },
    "showSubtasks": true,
    "nesting": true
  }
}
```

filters 条目与 URL `f=` 编码同构（field/op/values 三元组，values 含 `none` 哨兵与阶梯码）；display 键即面板四 section 状态。后端不解释、不校验内容（仅要求 JSON object）。**jsonb 归一化注（V1 切片实测登记）**：jsonb 存储归一化键序/空白，读回与写入字节不等价但语义等价；前端偏离比对（currentState vs view.config）必须解析后对象深比较，禁字符串比对。

### 4.3 接口（api.md §10）

| 接口 | 说明 |
|------|------|
| GET /workspaces/:wid/views?surface=&entityType=&projectId= | 列表；surface 必传；entityType 仅 views_page 需要；projectId 仅 project_issues 需要（必传） |
| POST /workspaces/:wid/views | {name, description, surface, entityType, config, projectId?}；name trim 非空；**不做重名约束** |
| GET /workspaces/:wid/views/:viewId | 单条（视图详情页用） |
| PATCH /workspaces/:wid/views/:viewId | name / description / config（presence 三态） |
| DELETE /workspaces/:wid/views/:viewId | 硬删除 |

### 4.4 页面级 view（列表页 tab 行）

- tab 行 = 预设 tab（§1.4）+ 该页 surface 的 view tab（固定层叠图标 + name，图标选择器后置 P3）+ "+" 按钮（**打开行内新建 panel**，§1.9；SaveViewDialog 退役；draft 初值 filters 空 + display 继承当前页面）；保存成功后新 view **自动激活**。编辑期 tab 行出现**临时 view tab（虚线框 + 铅笔图标）**，Save 成功转正 / Cancel 消失。
- 点 view tab：URL 写 `view=<id>` + config.filters 物化为 `f=` 参数；config.display 载入内存。
- **view 激活下的修改走 §1.9 两条路径**：不进编辑态时 Save 分裂下拉（Save to this view / Create new view...）；编辑态（tab 右键 Edit）= 行内编辑 panel，编辑期沙箱 / 双按钮同 draft / transient 挂起恢复 / Reset 三档均按 §1.9。
- tab 右键菜单：Edit / Delete（确认弹窗）两项（Duplicate/Favorite/Copy link 裁剪，§1.10）。
- 偏离点：当前状态 ≠ view.config 时 tab 显示偏离点（写回后消失）。
- **project_issues 面**：tab 行 = 项目详情预设 tab + 项目级 view tab（查 surface=project_issues ∧ project_id=当前项目）；其余规则同页面级 view。

### 4.5 Views 页（workspace 级）

- 新路由 `/w/:wid/views` + 侧栏 Workspace 组 Views 入口；页头 Issues / Projects 双 tab（entity_type）+ New view 按钮。
- 行：视图图标 + name + description（对齐 Linear 行形态；**owner 列裁剪**，无登录体系）。
- New view：创建空 config view（surface=views_page、entityType=当前 tab）并跳转详情页。
- 详情页 `/w/:wid/views/:viewId`：按 config 渲染列表（复用 TaskGroupList / 项目列表组件与面板组件，状态由 config 初始化），页内可继续改 filter/display 并 Update view；页头显示 view name（可 rename）。
- 隔离查询：列表页 tab 只查 surface=当前页；Views 页只查 surface=views_page —— 双轨互不可见。

## 5. 变更清单摘要

- **DB**：迁移 v1.4（§4.1：surface 四值枚举 + project_id 列 + 索引改建）；DATABASE_DESIGN.md 升 v1.4。
- **后端**：`GET /tasks` / `GET /projects` / `GET /projects/:projectId/tasks`（项目详情 Issues tab）收 `f=` 条件列表参数，Go 层过滤引擎求值（internal/filter 包，§2.5）；新增 sqlc `ListProjectMemberIdsByProjectIds`；view 包新增（CRUD handler + sqlc 查询）；api.md §2.2 / §7 / §8 回写 + 新增 §10。
- **前端**：FilterPanel（**A2 切片已完成（2026-09）：tasks_page / projects_page / project_issues 三面接入，filter 菜单打开即预取值集（staleTime Infinity 命中缓存），toggleValues 抽公共纯函数**）、DisplayOptionsPanel、ViewEditPanel（行内编辑 panel，SaveViewDialog 方案退役）、ViewTabs（tab 行改造）、ViewsPage、ViewDetailPage、useViews hook；项目表头排序前端化；任务页 tree/flat 硬编码退役；i18n 中英全量新键。
- **退役清单（触发条件显式，代码 TODO 标记可 grep）**：~~A2 前端 tab 迁移 `f=` 完成 → 删 `legacyStatusConds` 与 task handler 的 `filter=` 分支（`TODO(P2-A2)`）~~ **已完成（2026-09，api.md §8 已回写）**；~~B 切片 ordering 前端化完成 → 删 `GET /projects` 的 `sort`/`order` 参数与 `sortProjects`（`TODO(P2-B)`）~~ **已完成（2026-09，api.md §2.2 / §7 已回写）**；退役时同步更新 api.md §2.2 / §8 遗留注。
- **验证**：端到端（过滤×操作符矩阵抽样、display 四 section、view 生命周期 create/apply/update/rename/delete、双轨隔离）+ 回归（P1 标签 chip、tab 预设行为、项目表头排序迁移、删除后导航）。

## 6. 实施切片拆解与进度（P2 全切片）

> P2 三件套按切片推进：filter/display 两件套 A→A2→B→C→project_tasks 已全部落地，视图持久化按 V1–V5 拆分（V1 完成）。**每片独立可验证**（后端片 = curl 矩阵，前端片 = tsc + 受控浏览器截图矩阵归档 verify-capture/）；片内不夹带其它片的改动。as-built 细节注见 §2/§3 各处，本节登记切片范围、关键落地项与验证索引。

### A 后端过滤契约（✅ 2026-09）

- 三个列表面（`GET /tasks` / `GET /projects` / `GET /projects/:projectId/tasks`）收 `f=` 条件列表参数；internal/filter 引擎：Parse 三级白名单静默丢弃 + Apply 条件间 AND，Spec（Ops/Ladders/FromNow 覆写）/ Val 两插槽（§2.5）；
- 操作符语义 v2、none 哨兵互斥规则、日期相对阶梯（cutoff Go 层算、ago/from now 按字段分方向）定稿并实施（§1.6/§2.3/§2.4）；
- `ListTasksByWorkspace` 的 statuses 数组参数退役；labels/members 联结数据求值前批量取齐（新增 `ListProjectMemberIdsByProjectIds`）；
- 验证：curl 矩阵（过滤×操作符抽样）；api.md §2.2 契约回写。

### A2 前端三面 Filter（✅ 2026-09）

- tasks_page / projects_page / project_issues 三面接入 FilterButton + FilterPanel：纯添加级联菜单（DropdownMenu + Radix Sub 悬浮飞出、Dates 三级飞出）、chip 行三段可点 + ×/+/Clear/Save 预留（§2.1）；
- tab 迁移 `f=`：Active/Backlog/All 作隐式基底作用域合成（§1.4）；后端 `filter=` 遗留别名与 `legacyStatusConds` 退役（`TODO(P2-A2)` 完成，api.md §8 回写）；
- f= 镜像进 URL、react-query 键含 f（filter 发请求、display 不发）；菜单打开即预取值集（staleTime Infinity）、toggleValues 抽公共纯函数；
- 验证：浏览器矩阵归档 verify-capture/ verify-tf-*（tasks 面）、a2p-* / a2v2-*（projects 面）——菜单飞出/chip 改值/操作符联动/none 互斥/URL 恢复/排序保持。

### B projects_page display（✅ 2026-09）

- 项目列表页 Display options 面板：Grouping（none/status/priority/lead/member/label/startDate/targetDate + Show empty groups + Timeframe 日期桶，§3.1）、Ordering 前端化（表头点击与面板共享状态，§3.2）、Display properties 10 chips（§3.3）；
- 后端 `GET /projects` 的 `sort`/`order` 参数与 `sortProjects` 退役（`TODO(P2-B)` 完成，api.md §2.2/§7 回写）；
- 组头形态同期落地（展示细节批③⑤，§7）：两级组头可折叠、“+” 预填组路径值且 `sticky right-0`；
- 验证：浏览器矩阵归档 p2d-* / p2e-*——两级分组/日期桶/空组开关/表头排序/Reset 快照。

### C tasks_page display + tree 规则④（✅ 2026-09，规则④成文 09-14）

- 任务列表页 Display 面板：展示样式行 / Grouping 默认 Status / Sub-grouping / Ordering（Manual = 后端基底序）/ Completed tasks All-None（仅 All tab）/ Show sub-issues + Nested / Show empty groups / Display properties 8 chips；**display 状态每 tab 一份**（内存，§3 as-built 注）；
- 树形节点展示规则④定稿成文（§2.6）：渲染集 = 锚点祖先链 ∪ 锚点完整子树、链条置灰/孤儿提根置灰/侧分支剪枝、completed=none 叠加层（独立 chained 集合防断链）；
- 引擎去冗余：buildTaskTreeIndex 共享索引（parentOf 兼作孤儿判定）、groupValueMap 一次分桶、taskChildVisible 谓词复用、EMPTY_KEPT 跳空组；
- 行右元信息固定列宽槽位（Linear 实测对齐，§3.3）；
- 验证：opt-*（断链修复场景 chainA→chainM→chainX + 回归）、align-*（136 行列位量化 distinct x 各=1）。

### project_tasks 接线（✅ 2026-09-14）

- 项目详情 Tasks tab 接入同一 TaskDisplayButton 面板：display 状态本面一份、Completed 行恒展示、按钮位 Filter 右侧；过渡期 fixedDisplay（有过滤自动切平铺）退役，tree/flat 交还用户双开关（§3 as-built 注）；
- 验证：tdisp-* 矩阵——按钮序/默认树/面板行/completed=none/平铺/Reset 蓝点/过滤下孤儿提根/tasks_page 回归。

### V1 后端壳（✅ 完成 2026-09-16）

- 迁移 00002：`view_surface` 四值枚举 + `surface` 列 + 可空 `project_id`（FK→project CASCADE）+ 索引改建 `(workspace_id, surface, entity_type)`；goose 至 v2；
- `queries/view.sql` 7 查询 + `internal/view` 包：CRUD handler（surface⇒entityType 一致性、project 归属/软删校验、config object 形状校验、PATCH presence 三态读-改-写）+ 路由挂载 + main.go 枚举类型注册；
- 契约回写：api.md §10 升为正式 View 接口契约（原后序索引移 §11）、DATABASE_DESIGN 升 v1.4（§4.9 全节刷新）；
- 验证：curl 矩阵 27/27 全中（空列表/缺参/错配/跨 workspace/三态/幂等删除/双轨列表隔离）+ config echo 比对；
- **实测发现登记**：jsonb 归一化键序/空白 → “原样返回”= 语义原样，前端偏离比对禁字符串比对（api.md §10 注、§4.2 注）。

### V2 页面级 view · 创建/激活路径（三面一次接：tasks_page / projects_page / project_issues）

1. `useViews` hook：按 surface 列表查询（project_issues 面带 projectId；react-query 键含 surface/projectId）；
2. **ViewTabs** tab 行改造：预设 tab + view tab（固定层叠图标 + name）+ “+” 按钮；项目详情 tab 行同接（surface=project_issues ∧ project_id=当前项目）；
3. **ViewEditPanel 新建模式**（§1.9 形态）：name 行（图标 + 输入 + 右靠 Cancel/Create view）+ Description 行 + chip 行（行尾右靠 filter/display 双按钮）；编辑期 tab 行临时 view tab（虚线框 + 铅笔）；
4. **编辑期沙箱**：panel 与顶部 filter/display 按钮同操作 draft（chip 单行落 panel、顶部 chip 行隐藏）；预览 = 仅 draft；transient 挂起、Save/Cancel 后恢复；draft 初值 = filters 空 + display 继承当前页面；
5. **view 激活**：点 view tab 写 URL `view=<id>` + config.filters 物化为 `f=` + config.display 载入内存；
6. **偏离点**（currentState vs view.config 深比较）+ chip 行 Save 升级分裂下拉（Save to this view = PATCH config ← currentState；Create new view... = 开新建 panel 保存后切换激活）；
7. **Reset 档落地**：view tab 内 = 该 view 的 config.display（预设 tab = 全局默认，现状不变）。

验证矩阵：创建→激活→偏离→写回→分裂 Save→双轨隔离抽样 + 回归（filter/display 面板现状、tab 预设行为）。

### V3 编辑/删除路径

- tab 右键菜单：Edit / Delete（确认弹窗）两项（§1.10 裁剪项不渲染）；
- **ViewEditPanel 编辑模式**：draft = view.config 深拷贝、name/description 行内可改；Save = 元数据+config 一起 PATCH 提交并退出、view 激活、transient 恢复；Cancel = draft 全弃、transient 恢复；沙箱规则同 V2；
- **编辑期 Reset** = 进入编辑时基线（Edit = config.display）；
- 带偏离切 tab/导航 = 丢弃偏离（filters 部分随 URL 走）。

验证矩阵：右键菜单、编辑 Save/Cancel、Reset 三档全矩阵、偏离丢弃。

### V4 Views 页（workspace 级）

- 路由 `/w/:wid/views` + 侧栏 Workspace 组 Views 入口；页头 Issues / Projects 双 tab（entityType）+ New view 按钮（创建空 config view 并跳详情）；
- 行：层叠图标 + name + description（owner 列裁剪）；只列 surface=views_page（双轨隔离）；
- 详情页 `/w/:wid/views/:viewId`：按 config 渲染列表（复用 TaskGroupList / 项目列表组件与面板组件，状态由 config 初始化）；页内改 filter/display + Update view；页头 view name 可 rename。

验证矩阵：双 tab 列表隔离、New view 跳转、详情页渲染与写回、rename。

### V5 回归与回写

- view 生命周期 e2e：create/apply/update/rename/delete 全链；双轨隔离矩阵（四面互不可见 + 项目级 scope）；
- 回归：P1 标签 chip、tab 预设行为、项目表头排序、删除 view 后导航复位；
- 回写：README P2 状态、本节进度注、api.md 如有契约微调。

## 7. 本阶段不含（后序索引）

- **展示细节优化批（P2-B 后新开阶段，projects_page 列表视觉/交互对齐 Linear，2026-09 用户立项）**：~~① Display options 按钮移至 Filter **右侧**（对齐 Linear 页头按钮顺序）~~ **已完成（2026-09）**；~~② 去掉列名下方与每行下方的横线，**仅二级分组头行保留横线**（与一级灰底组头区分）~~ **已完成（2026-09）**；~~④ 表格列溢出窗口时横向滚动条**固定在右侧内容区（视口）底部**，而非表格底部~~ **已完成（2026-09）**；~~⑥ 面板底部 **Reset** 按钮：回到**全局默认**（蓝点熄灭态：不分组/手动/默认六列等，清掉本页会话全部 display 改动；用户定案更正：非开面板快照；2026-09 修订：按钮去外框纯文字形态）~~ **已完成（2026-09）**；~~③ 每一级分组头可折叠（chevron 收起/展开组内行）~~ **已完成（2026-09）**；~~⑤ 每个分组头右侧加 “+”，点击打开创建项目弹窗并**预选该分组属性值**（等价 New project 但预填分组值；二级组含一级值、日期桶预填桶首日；2026-09 修订：NONE 组也展示 “+” 且预填 = 仅继承组路径；“+” 常显（不靠 hover）且 `sticky right-0` 钉在横向滚动视口右缘，列溢出时无需拖到内容最右）~~ **已完成（2026-09）**。
- **P3**：看板拖拽、行内编辑、标签就地创建、~~分组折叠~~（**展示细节批③已提前落地 projects_page 面**）、日期绝对区间（Custom date）、localStorage 跨重启保持、~~Sub-grouping 二级分组、Show empty groups~~（**B 切片已提前落地 projects_page 面**，tasks_page 面仍留 P3）、组序方向切换、hidden-by-filters 隐藏计数空态、视图图标选择器、逐值命中计数。
- **P4**：视图 owner/visibility（私有/共享）、注册登录、多租户。
